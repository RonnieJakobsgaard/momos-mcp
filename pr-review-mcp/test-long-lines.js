// TEMP: long-line test file — delete before merge
const reallyLongVariableName = "this is a very long string that goes on and on and on and on and on and should definitely cause horizontal scrolling in the diff viewer when rendered";
const anotherLongLine = someFunction(argumentOne, argumentTwo, argumentThree, argumentFour, argumentFive, argumentSix, argumentSeven, argumentEight, argumentNine);
function processUserDataWithLotsOfParametersAndAVeryLongFunctionName(userId, userName, userEmail, userRole, userPermissions, userMetadata, userPreferences, userSettings) {
  return { userId, userName, userEmail, userRole, userPermissions, userMetadata, userPreferences, userSettings, timestamp: Date.now() };
}
const result = processUserDataWithLotsOfParametersAndAVeryLongFunctionName("abc123", "Ronnie", "ronnie@example.com", "admin", ["read", "write", "delete"], {}, {}, {});
